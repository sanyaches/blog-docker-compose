# Instructions

1) Create database with database-init.sql
2) Install all dependencies `yarn install`
3) Create file .env with connection details
4) Run server.js `node server.js`
```
// .env expample
POSTGRES_HOST=blog-postgres
POSTGRES_PORT=5432
POSTGRES_DB=blog
POSTGRES_USER=postgres
POSTGRES_PASSWORD=228sanya228
```
